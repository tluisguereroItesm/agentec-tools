[project]
name = "doc-reader"
version = "0.1.0"
description = "Tool reusable para extracción de texto de documentos locales (PDF, DOCX, XLSX, TXT, MD)"
requires-python = ">=3.11"
dependencies = [
  "pdfplumber>=0.11.0",
  "python-docx>=1.1.0",
  "openpyxl>=3.1.0",
]
